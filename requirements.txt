# AVG Publicatie Scanner — afhankelijkheden (MVP, alleen open-source)
requests>=2.31
beautifulsoup4>=4.12
lxml>=5.0
PyMuPDF>=1.24        # PDF: tekstlaag + metadata (import: fitz)
python-docx>=1.1     # .docx
openpyxl>=3.1        # .xlsx lezen + Excel-rapport schrijven
python-pptx>=0.6.23  # .pptx
tqdm>=4.66           # voortgangsbalk
PyYAML>=6.0          # config

# Optioneel — alleen nodig voor OCR (ocr.enabled / --ocr): ingescande documenten
# zonder tekstlaag alsnog lezen. Zwaar (onnxruntime), daarom niet in de basis-install:
#   pip install rapidocr-onnxruntime
# rapidocr-onnxruntime>=1.3
